import { useState } from "react";
import AuthComponent from "../features/auth/AuthComponent";

const Login = () => {
  return (
    <div>
      <AuthComponent />
    </div>
  );
};
export default Login;
